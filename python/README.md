### Change into the python folder and generate a default-env.json file with __cf de __
```
pip install -r requirements.txt
```

### When running locally, inject the PORT variable into the environment
```
export PORT=8080
```